# Safe Development Rules

- Run the relevant tests for every change.
- Require review before changes are merged.
- Never push directly to `main` or `develop`.
- Never expose secrets in source code, logs, commits, or messages.
- Preserve user changes and do not overwrite or revert them.
- Request explicit approval before performing sensitive or destructive actions.
